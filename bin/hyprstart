#!/bin/bash

# polkit
# provided by hyprpolkitdagent systemd user service
#/usr/libexec/polkit-mate-authentication-agent-1 &

# services
# need a custom user service
thunar --daemon &
# replaced by custom pypr servie
# pypr &

# theme
hyprtheme &

# wallpaper &
# enabled by user service
# hyprpaper &

# idle daemon
# enabled by user service
# hypridle &

# xdg
dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP &
systemctl --user start xdg-desktop-portal-hyprland &

# cliphist
# replaced by custom systemd user service
# wl-paste --type text --watch cliphist store --no-newline &
# wl-paste --type image --watch cliphist store &

# git
# dbus-update-activation-environment --all &
# gnome-keyring-daemon -r -d &

# replace by hyprsunset service
# hyprshade on ~/.config/hypr/shaders/blue-light-filter.glsl &

# reload hyprland plugins
hyprpm reload -n &
